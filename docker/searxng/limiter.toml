# SearXNG rate limiter — disabled for local aichat use
[real_ip]
x_for = 1
ipv4_prefix = 32
ipv6_prefix = 48

[botdetection.ip_limit]
link_token = false

[botdetection.ip_lists]
pass_ip = ["0.0.0.0/0", "::/0"]
